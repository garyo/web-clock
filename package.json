{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "lint": "yarn run eslint --ext .ts,.vue .",
    "lint-fix": "yarn run eslint --fix --ext .ts,.vue .",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "push-dir --dir=.output/public --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "eslint": "^8.29.0",
    "eslint-plugin-vue": "^9.8.0",
    "nuxt": "3.0.0",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "@mdi/font": "^7.0.96",
    "luxon": "^3.1.1",
    "sass": "^1.56.2",
    "vuetify": "^3.0.4"
  }
}
